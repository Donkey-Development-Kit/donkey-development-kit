{/*
  Shared partial: the standard closing "See the error taxonomy…" note.

  Used by the framework pages whose proxy rejections surface as typed exceptions
  and whose only page-specific footnote is this sentence: strands, llamaindex,
  openai, anthropic. Imported via
  `import AdapterErrorTaxonomyNote from '.../components/adapter-error-taxonomy-note.mdx'`.

  Intentional overrides that keep their own inline wording (documented, not silent):
    • langgraph        — prefixes a proxy-surface caveat before this sentence.
    • adk, crewai      — "surface through LiteLLM's error path" (LiteLLM owns transport).
    • agent-framework  — references the PolicyViolation hierarchy / middleware shape.
*/}

See the [error taxonomy](/errors) for how proxy rejections surface as typed
exceptions, and the [verification ledger](https://github.com/Donkey-Development-Kit/donkey-development-kit/blob/develop/docs/verified-apis.md) for
the current status of every constructor signature this adapter depends on.
